import 'package:flutter/material.dart';

class WorkTaskScreen extends StatelessWidget {
  const WorkTaskScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Work'),
      ),
    );
  }
}
